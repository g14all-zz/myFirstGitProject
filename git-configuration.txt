https://github.com/g14all/myFirstGitProject.git

git@github.com:g14all/myFirstGitProject.git

-- to configure username
git config --global user.name "jeevan"

- to configure email
git config --global user.email "g1_4all@hotmail.com"

-- to get the status
git status

git status -s

-- to add current dir
git add .

-- to add file
git add git-configuration.txt

-- to remove from index/cache
git rm --cached readme.txt

-- to 

-- git commit 
git commit -m "<message>"

got commit -a -m "<message>"

-- to get the differences
git diff
git diff --cached

git log --oneline

ssh-keygen -t rsa -C "g1_4all@hotmail"

ssh -T git@github.com

git remote add origin git@github.com:g14all/myFirstGitProject.git

git push origin master



